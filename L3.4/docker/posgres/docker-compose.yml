services:
  postgres:
    image: postgres:17
    container_name: postgres-images
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    volumes:
      - pgdata_image_proc:/var/lib/postgresql/data

volumes:
  pgdata_image_proc:
